{
  "name": "dezhou",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Web Texas Hold'em with LLM-driven AI opponents (OpenCode Go provider)",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js",
    "test": "node --test test/"
  },
  "engines": {
    "node": ">=20"
  }
}
